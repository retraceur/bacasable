/**
 * Constants used throughout the bacÀsable codebase.
 *
 * @module constants
 * @since 0.9.0
 */

/**
 * The hidden folder name for storing WP Now related files.
 */
export const BAC_A_SABLE_HIDDEN_FOLDER = '.bacasable';

/**
 * The file name for the SQLite plugin name.
 */
export const SQLITE_FILENAME = 'sqlite-database-integration-main';

/**
 * The URL for downloading the "SQLite database integration" WP Plugin.
 */
export const SQLITE_URL =
	'https://github.com/wordpress/sqlite-database-integration/archive/refs/heads/main.zip';

/**
 * The default starting port for running the WP Now server.
 */
export const DEFAULT_PORT = 8881;

/**
 * The default PHP version to use when running the WP Now server.
 */
export const DEFAULT_PHP_VERSION = '8.0';

/**
 * The default Retraceur version to use when running the WP Now server.
 */
export const DEFAULT_RETRACEUR_VERSION = 'latest';

/**
 * The URL for downloading the "wp-cli" WP cli.
 */
export const WP_CLI_URL =
	'https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar';
